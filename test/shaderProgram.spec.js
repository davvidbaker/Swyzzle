// doesn't work
/*
// using chai should assertion style
import chai from 'chai';
chai.should();

// import sample shaders
import {vertexShader, fragmentShader} from '../shaders/idle/og.js';

import ShaderProgram from '../fluid-sim/ShaderProgram.js';

// this is just an example of what a test looks like
describe('Array', () => {
  describe('#indexOf()', () => {
    it('should return -1 when the value is not present', () => {
      [1,2,3].indexOf(4).should.equal(-1);
    });
  });
});


describe('ShaderProgram', () => {
  let shaderProgram;
  before(() => {
    shaderProgram = new ShaderProgram('wassup',gl, vertexShader, fragmentShader);
  });
  
  describe('#exists', () => {
    it('should exist', () => {
      shaderProgram.should.exist;
    });
  });
});

*/